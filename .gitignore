# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editors / IDE ────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── Node / tooling (in case scripts are added) ───────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local

# ── Build / temp ─────────────────────────────────────
dist/
build/
.cache/
tmp/

# ── Keep design source files out of the repo if heavy ─
# assets/*.psd
# assets/*.ai

# ── External source material (raw generations, kits, drafts) ─
# Working material to pick from — never published with the profile.
external/
